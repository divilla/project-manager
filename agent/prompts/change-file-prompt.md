Take my initial ideas from `agent/changes/106-db-changes-ref-slug-insert-seed.md` and turn them into a complete Change specification using the exact structure from `agent/prompts/change-file-structure.md`.

First, clarify any missing details that are required to make the Change testable. If the idea is clear enough, make reasonable assumptions and state them in the Change file.

Use concise, product-focused language. Keep the Change scoped to one outcome. Requirements and Acceptance Criteria must be observable and testable. Use existing project vocabulary from `docs` when relevant. Add realistic Verification commands and QA
Test Cases. Put out-of-scope ideas in Non-Goals or Follow-Ups instead of expanding the Change.

Do not implement anything. Only produce the Change specification.
