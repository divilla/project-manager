I'd like to build AI powered software development project management tool. I'd like to build a prototype first, then use it for some time, then build full blown solution. Prototype version should have most of the features, but should not have user management, task assignements etc. The tool must be in the form of web application, that means it has frontend built vuejs/quasar and backend built in go/echo. Database prototype is in postgresql://localhost:5432/postgres. The idea behind the whole project to have completeness instead of time estimations, so it should have some sort of dashboard showing completeness of tasks groupped by task_phase. Top menu should have Home/Planning/Projects/Help menus. I'd like you to build me a plans and specs so that I can review them, if you have some ideas of something to add please do and use folders plans and specs to save all the documents. Please name all plans and specs like 01-introduction.md style. Do your best, I'm expecting 10x product owner of you here. DO NOT BUILD ANYTHING - just create documentation. If you need to create folder for some other documents, please feel free to do so.

Update: the project already has a database. All implementation and agent work must use the existing database as-is. Do not create migrations, alter schema, seed lookup data, or change `task_phase` and `task_type`; those tables already contain all possible options.

Update: `task` and `requirement` include created/modified timestamps. The database also has `task_history` and `requirement_history`. Before any user or AI update/delete to `task` or `requirement`, save the current version into the matching history table. For deletes, save the history row with `deleted = true`.
