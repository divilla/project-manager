# Revisit API tests

- Examine if all API endpoints are well backed with API - Integration Tests
- Add missing tests update existing ones make them great

After implementation always run:
- backend/ make api-test
- run make lint after every code update
- cli/ make test
- frontend/
